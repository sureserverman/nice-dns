# Domains blocked exactly (in addition to anything matched by the adlists in
# adlists-default.txt). Seeded into Pi-hole on first install by
# scripts/seed-pihole.sh via `pihole denylist add <domain>`.
#
# Lines beginning with '#' are comments. Blank lines are ignored.
# After install, manage entries via the Pi-hole web UI or
#   docker exec pi-hole pihole denylist add|remove <domain>

www.googleadservices.com
ad.doubleclick.net
googleads.g.doubleclick.net
us-central1-adaptive-growth.cloudfunctions.net
chatgpt.com
